# After creating your GitHub repository, run these commands:

# Replace YOUR-USERNAME with your GitHub username
git remote add origin https://github.com/YOUR-USERNAME/tudum.git
git branch -M main
git push -u origin main

# Example:
# git remote add origin https://github.com/john/tudum.git
# git branch -M main
# git push -u origin main
